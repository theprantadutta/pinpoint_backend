
services:
  # PostgreSQL database (optional - you're using external DB)
  # Uncomment if you want local database for development
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: pinpoint
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Backend API
  backend:
    build: .
    ports:
      - "84:8000"
    env_file:
      - .env
    # depends_on:
    #   db:
    #     condition: service_healthy
    restart: unless-stopped
    volumes:
      - ./app:/app/app  # Mount app directory for hot reload
      - ./alembic:/app/alembic  # Mount alembic for migrations
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Redis (optional, for caching and background tasks)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:
